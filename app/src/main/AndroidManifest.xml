<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.surfstudio.summerschool">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name="ru.surfstudio.summerschool.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="ru.surfstudio.summerschool.lifecycle.main.LifecycleActivity"
            android:exported="true" />

        <activity
            android:name="ru.surfstudio.summerschool.lifecycle.fixed.LifecycleFixedActivity"
            android:exported="true" />

        <activity
            android:name="ru.surfstudio.summerschool.lifecycle.usual.LifecycleUsualActivity"
            android:exported="true" />

        <activity
            android:name="ru.surfstudio.summerschool.lifecycle.viewmodel.LifecycleViewModelActivity"
            android:exported="true" />

        <activity
            android:name="ru.surfstudio.summerschool.service.PlayerServiceActivity"
            android:exported="true" />

        <service
            android:name="ru.surfstudio.summerschool.service.PlayerService"
            android:exported="false" />
    </application>

</manifest>